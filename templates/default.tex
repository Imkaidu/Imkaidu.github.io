\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{graphicx}
% \usepackage{fontspec}
% \usepackage{xunicode}
% \usepackage{xltxtra}
\usepackage{listings}
\usepackage{textcomp}

\usepackage{enumitem}

% Unicode character definitions. Kept inline (rather than in the shared
% header/unicode-chars.tex) because this template has no $header-includes$
% placeholder, so pandoc's -H content is silently dropped for the PDF
% build -- see header/unicode-chars.tex for the fuller explanation and the
% copy the slides build uses via -H. Keep the two lists in sync by hand.
\usepackage{newunicodechar}
\newunicodechar{●}{\textbullet}
\newunicodechar{├}{\textbar}
\newunicodechar{└}{\textbar}
\newunicodechar{─}{-}
\newunicodechar{→}{\ensuremath{\rightarrow}}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}
$if(title)$
\title{$title$}
\maketitle
$endif$

$body$

\end{document}